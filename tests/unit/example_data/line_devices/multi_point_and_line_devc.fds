# Add some non-DEVC namespaces, to check parser ignores these:

# Define name of simulation, which will be the prefix of all output files
&HEAD CHID='no_vents', TITLE='no_vents' /

# Define the room as a 3m x 4m x 3m cube
&MESH XB= 0, 3, 0, 4, 0, 3, IJK= 30, 40, 30 /

# Simulate the fire for 5 seconds, output results every second
&TIME T_END=5. /
&DUMP NFRAMES=5, WRITE_XYZ=.TRUE. /

# Define the type of fuel for the fire
&REAC ID = 'propane reaction',
SOOT_YIELD=0.03,
CO_YIELD=0.05,
FUEL='PROPANE'/

# Create an obstruction which will function as the fire, HRRPUA = Heat Release Rate per Unit Area
&OBST XB= 1.3, 1.7, 1.8, 2.2, 0, 0.5, COLOR='RED', SURF_ID='fire1' /
&SURF ID='fire1', HRRPUA=150 /




# Define a point DEVC device
&DEVC XB=1.5,1.5,0,0,1.5,1.5, QUANTITY='VISIBILITY', ID='visibility_point' /

# Define line DEVC devices
&DEVC XB=2,2,1,3,2,2, QUANTITY='TEMPERATURE', ID='temp_line_y', POINTS=20,
Y_ID="y_coords", STATISTICS_START=1., STATISTICS_END=5. /

# Define point DEVC
&DEVC XYZ=1.5,0,1.5, QUANTITY='TEMPERATURE', ID='air_temperature_point' /
&DEVC XBP=1.5,1.5,0,0,1.5,1.5, QUANTITY='U-VELOCITY', ID='velocity_point' /

# Define another line DEVC using HIDE_COORDINATES, should use coords from before point DEVCs
&DEVC XB=2,2,1,3,2,2, QUANTITY='U-VELOCITY', ID='velocity_line_y', POINTS=20, HIDE_COORDINATES=T
Y_ID="y_coords", STATISTICS_START=1., STATISTICS_END=5. /

&TAIL /